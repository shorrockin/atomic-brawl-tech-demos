(function() {
  define(['constants', 'logging'], function(constants, log) {
    var Character;
    return Character = (function() {
      function Character(x, y) {
        this.x = x;
        this.y = y;
      }
      return Character;
    })();
  });
}).call(this);
